#pragma once

#include "elevio.h"


typedef ButtonType OrderType;

//algoritme / high level driver

int a = elevio_callButton(8, );

OrderType find_ordertype(OrderType order);


switch (OrderType)
{
case OrderType.HALL_DOWN:
    /* code */
    break;

default:
    break;
}